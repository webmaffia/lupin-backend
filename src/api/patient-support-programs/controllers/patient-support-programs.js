'use strict';

/**
 * patient-support-programs controller
 */

const { createCoreController } = require('@strapi/strapi').factories;

module.exports = createCoreController('api::patient-support-programs.patient-support-programs');
