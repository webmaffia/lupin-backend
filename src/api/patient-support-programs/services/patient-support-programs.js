'use strict';

/**
 * patient-support-programs service
 */

const { createCoreService } = require('@strapi/strapi').factories;

module.exports = createCoreService('api::patient-support-programs.patient-support-programs');
