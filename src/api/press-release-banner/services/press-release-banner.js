'use strict';

/**
 * press-release-banner service
 */

const { createCoreService } = require('@strapi/strapi').factories;

module.exports = createCoreService('api::press-release-banner.press-release-banner');
