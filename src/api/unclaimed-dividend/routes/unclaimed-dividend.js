'use strict';

/**
 * unclaimed-dividend router
 */

const { createCoreRouter } = require('@strapi/strapi').factories;

module.exports = createCoreRouter('api::unclaimed-dividend.unclaimed-dividend');
