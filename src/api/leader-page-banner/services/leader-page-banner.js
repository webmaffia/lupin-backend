'use strict';

/**
 * leader-page-banner service
 */

const { createCoreService } = require('@strapi/strapi').factories;

module.exports = createCoreService('api::leader-page-banner.leader-page-banner');
